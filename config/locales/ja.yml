ja:
  time:
    formats:
      default: "%Y/%m/%d %H:%M:%S"
      onlytime: "%X"
      #view内に応じてフォーマットの設定を変更
  #f.date_select用追加設定
  date:
    order:
      - :year
      - :month
      - :day
#出品時のエラーメッセージ
ja:
  activerecord:
    errors:
      models:
        item:
          attributes:
            ctg_id:
              other_than: "カテゴリーは必ず選んで下さい。"
